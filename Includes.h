#ifndef INCLUDES_H
#define INCLUDES_H

#include <string>
#include <Windows.h>
#include <iostream>
#include <exception>
#include <time.h>
//#include <regex>
//#include <memory>
#include <vector>
#include <bitset>
#include <algorithm>
#include <cmath>
#include <random>
#include <functional>
#include <iterator>
#include <conio.h>

using namespace std;
/*
using byte = unsigned char;
using bit = bool;

using bit_vect_iter = std::vector<bit>::iterator;
using byte_vect_iter = std::vector<byte>::iterator;

using length_t = unsigned long int;
*/

typedef unsigned char byte;
typedef bool bit;
typedef std::vector<bit>::iterator bit_vect_iter;
typedef std::vector<byte>::iterator byte_vect_iter;
typedef unsigned long int length_t;

#endif //INCLUDES_H